const Footer = () => {
  return (
    <footer className="fixed bottom-0 w-full bg-primary text-secondary py-2 flex justify-center items-center">
      Footer
    </footer>
  );
};

export default Footer;
