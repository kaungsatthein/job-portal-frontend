const Footer = () => (
  <footer className="fixed bottom-0 w-full bg-primary text-secondary py-2 text-center text-sm">
    © {new Date().getFullYear()} HireHub — Empowering Careers, Connecting
    Talent.
  </footer>
);

export default Footer;
